function mostrar()
{

    var clave = prompt("ingrese el número clave.");
    var pass = "utn750";

    while (clave != pass) {
        clave = prompt("Clave incorrecta. Vuelva a intentar.");
    }

}//FIN DE LA FUNCIÓN
